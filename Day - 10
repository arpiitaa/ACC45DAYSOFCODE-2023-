#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T; 

    while (T--) {
        int X, Y;
        cin >> X >> Y; 
        
        int maxChocolates = X / Y; 
        cout << maxChocolates << endl;
    }

    return 0;
}
